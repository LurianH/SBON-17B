'use client';import Link from 'next/link';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Login(){const [msg,setMsg]=useState('');async function login(fd:FormData){const db=createClient();if(!db){setMsg('Modo de desenvolvimento: Supabase não configurado.');return}const {data,error}=await db.auth.signInWithPassword({email:String(fd.get('email')),password:String(fd.get('password'))});if(error){setMsg('E-mail ou senha inválidos.');return}const {data:profile}=await db.from('profiles').select('active').eq('id',data.user.id).maybeSingle();if(!profile?.active){await db.auth.signOut();setMsg('Usuário sem perfil ativo. Solicite acesso ao administrador.');return}location.href='/'}return <div className="login-page"><div className="login-card"><div className="brand"><span className="mark">V</span><span><b>vitalux</b><small>ECOATIVA</small></span></div><span className="eyebrow">SBON 17B</span><h1>Acesso ao painel</h1><p>Entre com seu e-mail corporativo.</p><form action={login} className="data-form"><label>E-mail<input name="email" type="email" autoComplete="email" required/></label><label>Senha<input name="password" type="password" autoComplete="current-password" required/></label><button>Entrar</button><span className="form-message">{msg}</span></form><Link className="login-link" href="/login/forgot-password">Esqueci minha senha</Link><small>O cadastro público está desabilitado.</small></div></div>}
