'use client';
import {useState} from 'react';import {AppShell} from '@/components/AppShell';import {FinancialForm,WeeklyForm} from '@/components/EntryForms';
export default function Atualizar(){const [tab,setTab]=useState<'weekly'|'financial'>('weekly');return <AppShell title="Atualizar dados" eyebrow="OPERAÇÃO"><div className="tabs"><button className={tab==='weekly'?'selected':''} onClick={()=>setTab('weekly')}>Atualização Semanal</button><button className={tab==='financial'?'selected':''} onClick={()=>setTab('financial')}>Atualização Financeira</button></div><section className="panel form-panel"><h2>{tab==='weekly'?'Atualização Semanal':'Atualização Financeira'}</h2><p className="hint">{tab==='weekly'?'Informe os valores físicos acumulados. Os avanços serão calculados automaticamente.':'Registre um único faturamento por competência.'}</p>{tab==='weekly'?<WeeklyForm/>:<FinancialForm/>}</section></AppShell>}
