'use client';
import {useState} from 'react';import {AppShell} from '@/components/AppShell';import {FinancialForm,WeeklyForm} from '@/components/EntryForms';
export default function Atualizar(){const [tab,setTab]=useState<'weekly'|'financial'>('weekly');return <AppShell title="Atualizar dados" eyebrow="OPERAÇÃO"><div className="tabs"><button className={tab==='weekly'?'selected':''} onClick={()=>setTab('weekly')}>Atualização Semanal</button><button className={tab==='financial'?'selected':''} onClick={()=>setTab('financial')}>Atualização Financeira</button></div><section className="panel form-panel"><h2>{tab==='weekly'?'Atualização Semanal':'Atualização Financeira'}</h2><p className="hint">{tab==='weekly'?'Informe os valores físicos acumulados consolidados. A separação Água/Esgoto só será habilitada quando houver dados oficiais segmentados.':'Registre o valor oficial de uma única competência mensal; não informe o acumulado sem rateio.'}</p>{tab==='weekly'?<WeeklyForm/>:<FinancialForm/>}</section></AppShell>}
